

<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <span class="navbar-brand" href="#">Catan Count</span>
    <ul class="nav navbar-nav">
      <li><%= link_to "New Game", games_path, method: :post %></li>

      <% if current_user && !current_user.is_guest? %>
        <li><%= link_to "Sign Out", signout_path %></li>
      <% else %>
        <%# not logged in %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Login <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Facebook", "/auth/facebook" %></li>
            <li><%= link_to "Google", "/auth/google_oauth2" %></li>
          </ul>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

